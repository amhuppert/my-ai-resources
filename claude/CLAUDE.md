<critical>
If changing a Claude plugin (new commands/skills/agents/etc. or updates to existing ones), you MUST bump the version number in the plugin.json file.

For the ai-resources plugin, the version number must be updated in the claude/ai-resources-plugin/.claude-plugin/plugin.json file.
</critical>
