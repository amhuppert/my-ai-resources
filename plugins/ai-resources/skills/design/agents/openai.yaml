interface:
  display_name: "Design"
  short_description: "Create or iterate on a software design via a multi-agent..."
  default_prompt: "Use $design to create or iterate on a software design via a multi-agent workflow with parallel research and review phases."
policy:
  allow_implicit_invocation: false
